import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ContrasegnaolvidadaPage } from './contrasegnaolvidada.page';

describe('ContrasegnaolvidadaPage', () => {
  let component: ContrasegnaolvidadaPage;
  let fixture: ComponentFixture<ContrasegnaolvidadaPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ContrasegnaolvidadaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
